[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-documents-rag"
version = "1.0.0"
description = "Enterprise Document Intelligence RAG Platform"
readme = "README.md"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "src.api:app"
